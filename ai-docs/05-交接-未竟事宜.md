# 交接 · 未竟事宜

> 用途：上下文压缩 / 跨会话接续。**只记录"还没做完、且已查清根因"的事**，
> 不带猜测。当前基线见 §0；每条都给了「症状 → 根因 → 改哪几个文件 → 怎么验」。
> **已完成的条目保留原文并标注 ✅**（连同当时的根因与验证方式），便于回溯。

---

## 0. 当前基线

| 项 | 值 |
|---|---|
| 本地 HEAD == `origin/main` | `bbd1413`（本文件写入时） |
| 工作树 | 干净 |
| `pnpm run verify` | **0**（六包 build + typecheck + 1079 用例） |
| 分项测试 | protocol 21 / game-core 400 / transport 73 / ui-kit 194 / server **314** / web **76** |
| 冒烟（需数据库） | 线协议 15/15、路由 20/20、完整游戏流程 22/22、拾取规则 18/18、进图剧情 14/14、**角色归属 10/10** |
| 数据库 | 独立库 `idle_dark`（**不动** xiuxian 的 `idle_game`），凭据在 `packages/server/.env`（gitignore） |
| Prisma | `packages/server/prisma/schema.prisma` 为数据设计真相；与线上库 `db pull` 反推结果**零语义差异** |
| 调试进程 | **已停止**（后端 3100 已关）。⚠️ **5273 上仍有一个非本会话启动的 Vite 在跑**，本会话的 bash 在独立 PID namespace 里（`ss` 看得到、`lsof`/`fuser`/`pkill` 都够不到）→ 要停请用户自行在自己的终端里 Ctrl-C。xiuxian 的 3000 未受影响（5173 本就已停） |

> ⚠️ **更新（R5-b 完成后 = R0–R5 收官，以代码实测为准）**：`pnpm run verify` = 0，**1214 用例**
> （protocol 21 / **game-core 433** / transport 73 / ui-kit 194 / **server 417** / web 76）；
> 数据库冒烟：路由 **32/32**、挑战队列 15/15、地图控制器 15/15、指标 9/9、线协议 15/15、
> 完整流程 22/22、拾取规则 18/18、进图剧情 14/14、角色归属 10/10。
> R0–R5 交付见 [`09-地图与秘境逻辑服.md`](09-地图与秘境逻辑服.md) **§0.4–§0.8**；
> 架构门禁跨服深路径白名单 **4 → 1**（仅剩 `dungeon → map`）。
> 顺带修了 game-core **既有缺陷**：`BattleWorld.sendSkillUsage` 缺失（数据技能调用即抛错，
> 离线秘境因此整段无收益）。残留：秘境端到端冒烟受前置剧情链限制（单测覆盖）、
> 07 的 T-A3/T-A4/T-B1/T-D1 未做、R8 多进程依赖框架运行时。

> ⚠️ **更新（2026-09：装备与伤害体系重构 E0–E7 全部落地 = 最新基线）**：
> `pnpm run verify` = 0，**1275 用例**（protocol 30 / **game-core 482** / transport 73 /
> ui-kit 194 / server 420 / web 76）。数据库冒烟（本会话另起的 **3101** 实例）：
> 路由 **32/32**、指标 9/9、线协议 15/15、完整流程 22/22、进图剧情 14/14、角色归属 10/10、
> 地图控制器 15/15、挑战队列 15/15、拾取规则 17/17（1 项随机掉落跳过）。
> 交付 = 9 个独立提交：E0 基线 / E0.5 词缀判空 / E1 属性三化 / E2 品质三档 /
> E3 词缀前后缀骨架 / E4 九槽与副手判定 / E5 元素分类 + dark→chaos + 词缀作用域 /
> E5.5 双持交替 / E6 掉落摘除与占位物品。**新约定见 `AGENTS.md` §18**。
> 后续更新：12 通货 + 12 精华已由占位升级为**真实物品**（取自修仙设计稿：12 通货去掉 `vaal` +
> 6 精华，另 6 个精华槽为空位），掉落稀有度见 `data/index.ts` 的 `CRAFT_DROP_RATES`。
> 本期未做（下期）：词缀池/底材分布（P5）、附加元素伤害（P6）、装备底材目录与购买 Action（P8）、
> 手工制作（P9）、特殊秘境特殊底子（P10）、`lootRule` 域退役（当前休眠保留）。

> ⚠️ **更新（数据层「视图类型撒谎」专项修复之后）**：`pnpm run verify` = 0，**1225 用例**
> （protocol 21 / **game-core 441** / transport 73 / ui-kit 194 / **server 420** / web 76）。
> 数据库冒烟（在本会话另起的 **3101** 实例上跑，未打扰用户 3100 的服务）：
> 路由 **32/32**、指标 9/9、线协议 15/15、完整流程 22/22、进图剧情 14/14、角色归属 10/10、
> 地图控制器 15/15、挑战队列 15/15、拾取规则 17/17（1 项随机掉落跳过）。
> 本轮共修 3 处同源运行期缺陷（§2.3 `unit.timeline`、§2.4 `sendGeneralMsg` + `SkillState.summoner`），
> 并新增**编译期门禁** `game-core/src/data/_shapes.gate.ts`（视图成员必须真实存在于内核上）。
> 详见 `AGENTS.md` **§17**。

**必须遵守的约定**都在 `AGENTS.md`（尤其 §1 硬约束、§5 线协议与两级错误判定、§7 环境坑、
§10 数据库、**§11 拾取规则编码**、**§12 剧情推进**、**§13 中立单位**、**§14 推送频率**、§15 角色会话归属）。
读代码前先读它，能省大量返工。

**容量/并发要先看 [`06-容量基准与并发上限.md`](06-容量基准与并发上限.md)**：那里有实测的
CPU/帧大小/内存/DB 数字，以及「新任务书必须先拍板的 7 个参数」（含**超载降级规则**）。
一句话：**战斗 CPU 只占单核 6%，但 `maxCharactersPerTick=100` 会让 5000 人时世界只有 50% 速度、
快照退化成 10 秒一帧** —— 这类"每轮处理 N 个"的循环都必须同时给全局预算与超载行为。

---

## 1. ✅ 已修复：拾取规则「自动出售/分解」不生效

**症状**：面板里设置「某类装备某品质 → 出售/分解」后，战斗掉落仍走 `minLootLevel` 兜底规则，
面板设置**完全不起作用**。

**根因（当时逐行确认，两侧键与值类型都不匹配）**：

- 面板侧编码（原 `server/.../inventory/internal/loots.ts`）：
  ```
  player.lootRule: Map<string, number>
     '__enabled__'                  → 1 | 0          （全局开关）
     `c:${class}:${quality}`        → action | action+10（停用则 +10）
  ```
- 战斗侧读取（原 `game-core/src/combat/battle-world.ts:553-565`）：
  ```ts
  const rule = this.player?.lootRule?.get(clazz);   // ← 按 class 取，取不到（键是 c:class:quality）
  const value = rule && rule[quality];              // ← rule 是 number，再索引 quality 必然 undefined
  // → 永远返回 0，于是继续走 minLootLevel
  ```
- 还有一个**被类型谎言掩盖**的问题：`combat/player-unit.ts` 的 `PlayerLike.lootRule` 被标成
  `Map<string, Record<number, number>>`（照抄原版 `Map<class, number[]>`），而 `Player.lootRule`
  实际是 `Map<string, number>` → `tsc` 完全看不出编码错配。

**已做的修法（采用"编码下沉 game-core 成为唯一定义"）**：

1. 新增 `packages/game-core/src/rules/loot-rule.ts`：`LootRuleAction` / `LOOT_RULE_ENABLED_KEY` /
   `LOOT_RULE_PREFIX` / `LOOT_RULE_DISABLED_OFFSET` / `lootRuleKeyOf` / `parseLootRuleKey` /
   `encodeLootRule` / `decodeLootRule` / `lootRuleEnabledOf` / `lootRuleActionOf`，并从
   `rules/index.ts` 导出（`game-core` 公共出口自动带出）。
2. `combat/battle-world.getLootRule()` 改为**单行委托** `lootRuleActionOf(this.player, …)`。
3. `server/.../inventory/internal/loots.ts` 删除本地那份编码/判定，改从 `@idle-dark/game-core` 引入；
   `lootrule/internal/loot-rule-ops.ts` 也直接引 `game-core`（面板只读写状态）。
4. `PlayerLike.lootRule` 类型改为 `ReadonlyMap<string, number>`。
5. **顺带修掉一个同链路的真 bug**：`world/internal/player-like.ts` 的 `lootRecorder` 在
   `player.loot(slot)` **之后**读 slot，而 `Player.loot` 会 `clear()` 它 → `(battle, loot)` 推送
   一直是空槽（`{key:null,count:0}`、`dto.gold` 丢失）。改为**先快照再落地**。

**验证（全部已实跑）**：

- 单测：`game-core/src/rules/loot-rule.test.ts`（19 例：key 编解码 / `+10` 停用边界 /
  `undefined`·`NaN`·`Infinity`·字符串 / 全局开关 / `minLootLevel` 边界 / 空 class）。
- 集成：`game-core/src/combat/loot-rule.test.ts`（10 例：从 `loots()` 一路断言到 `BattleSink.loot`
  事件的 `sell`/`decompose`/`pickup`）。**用旧实现跑会挂 5/10**，已实测。
- 服务端：`server/test/world-loot-rule.test.ts`（6 例：`opUpdateLootRule` 面板写入 → `toPlayerLike`
  → 真实 `RulesLootService` → `Player.loot` 落地）。**去掉快照修复会挂 2/6**，已实测。
- 端到端：`server/scripts/loot-rule-smoke.mjs`（真实 REST+WS+DB：注册 → 建角色 → 完成
  `eyer-stories-1` → 面板规则全设"出售" → 落库往返复核 → 进 `town.street` → 观察
  `(cmd=40,subCmd=2)` 掉落推送）。本地以 `LOOT_WINDOW_MS=300000` 实跑 **18/18 通过**，
  其中抓到一次真实装备自动出售：
  `{"slot":{"key":"gold","count":10,...},"handled":"sell","gold":10}`，
  且同期 `inventory.list` 里除已装备的「细木剑」外**没有多出任何装备**（确实被卖掉而非入包）。

**残留注意**：装备掉率是概率性的（`slime.minimal` 装备 0.1/kill），冒烟里那一项在窗口内没掉出装备时
记 SKIP 而非 FAIL（避免 CI 抖动）；`LOOT_WINDOW_MS` 可调（默认 90 s）。**该脚本未接入 CI**（要跑数分钟），
CI 的门禁由上面那批确定性单测/集成测试承担。

---

## 2. ✅ 已修复：纯剧情脚本的「进图自动播放」（以及击杀任务的静默登记）

**症状**：原版里「到达满足条件的地图即自动播放该图剧情」（`MapPanel.checkStories()`），
现在必须玩家自己到「故事」页点一下。
**更严重的是**：原版进图时会把 `kill` / `purchase` 类剧情**静默登记**为进行中并挂上击杀任务；
移植版只在玩家手动 `story.play` 时才登记 → 玩家不去点，打怪**完全不计进度**，主线直接卡死。

**已做的修法**（对照 `dark-forever-memorize/src/pages/battle/components/MapPanel.js:140-248`）：

1. `story/internal/story-ops.ts` 新增 `opAdvanceStoriesOnMapEntry(tables, player, extras, map)`
   与 `registerStoryTask(extras, story)`：
   - `kill` / `purchase` → 登记为 `task`（击杀任务挂 `killCount`），返回进 `tasks`；
   - 纯剧情脚本 → **不改状态**，返回进 `scripts`（打开时才置 `task`）。
   **幂等**：登记过的因 `status !== 'none'` 跳过，重复进图不会把击杀进度重置回满。
2. `opFinishStory` 用同一套分类：新就绪的 `kill`/`purchase` **当场登记**、纯剧情脚本上报
   （对应原版 `checkStories()` 的 `while (dirty)` 循环）。`opPlayStory` 复用 `registerStoryTask`。
3. `WorldService.start()`（会话首次落地在该图）与 `WorldService.enterMap()` 调用它，
   并推 `(story, unlock)`；`onEnemyKilled` 在剩余击杀数降到 0 时也推一次。
4. 推送载荷冻结为 `StoryUnlockDto`（`protocol/src/dto.ts`）：`{ key, name, taskType, autoPlay }`。
   `autoPlay=true` = 纯剧情脚本刚可开启 / 击杀任务刚达成。
5. 前端：`web/src/stores/ui-store.ts`（新）把「当前面板 key」从 `GameShellPage` 的局部 state
   提升为 Store —— 否则推送驱动的跳转无处发起；`story-store.handleNotification` 依据 `autoPlay`
   决定是否 `open()`，打开前先切到「stories」面板，玩家正在读别的剧情时不打断。

**验证（全部已实跑）**：

- 单测：`server/test/logic/story/story.test.ts` +9（进图推进/幂等/地图不匹配/解锁分类）；
  `server/test/world.service.test.ts` +4（start 推剧情 1、进图推剧情 2、登记剧情 3 击杀任务、
  击杀达成推送且不重复）。
- 集成：`web/test/integration.test.ts` +5（autoPlay 自动打开并跳面板、autoPlay=false 不跳、
  读剧中不打断、已完成不打开、载荷缺 key 安全忽略）；`web/test/unit.test.ts` +3（`UiStore` 边界）。
- 端到端：`server/scripts/story-entry-smoke.mjs`（真实 REST+WS+DB）**14/14 通过**，
  实测链路：进 `town.street` → 收到 `eyer-stories-2`（`script`/`autoPlay=true`）→ 完成它 →
  收到 `eyer-stories-3`（`kill`/`autoPlay=false`）→ `story.list` 显示 `status:'task'`、`remaining:10`。
- 顺带把 AGENTS 新增 **§12** 记录这套语义。

---

## 2.1 ✅ 已修复：中立（黄名）怪不可点选 → 主线第 4 段必卡死

**症状**：`eyer-stories-4` 的任务是**击杀 1 只大史莱姆**（`slime.giant`，`camp: 'neutral'`），
但玩家在「战斗」面板里**根本看不到它、也点不动它** → 任务永远停在「剩余 1」，主线卡死。

**根因（纯前端过滤）**：
- `web/src/stores/world-store.ts` 的 `enemies` 只筛 `camp === 'enemy'`，中立怪不进列表；
- `BattlePanel.focus()` 又有一行 `if (unit.camp !== 'enemy') return;` 把点击吞掉。
- 而服务端与内核**本来就是允许的**：`camps.ts` 的 `CampRelation.player.neutral === true`
  （可攻击但不自动选为目标）、`world.service.focus` 只拒绝 `ghost`、`focusEnemy` 用 `canAttack` 判定；
  原版剧情还专门教了这条规则——「黄色名字的魔物不会主动攻击英雄们，溅射和群体伤害也不会攻击
  他们。但如果英雄主动攻击他们，他们就会加入战斗」。

**修法**：
1. `world-store.ts` 新增纯函数 `isAttackableCamp(camp)`（`enemy | neutral`）与
   `get attackables()` / `get neutrals()`；`enemies` 只留作计数。
2. `BattlePanel` 的战场列表改用 `attackables`，中立怪带「中立」金标；`focus()` 改用
   `isAttackableCamp()`；计数加上「中立 N」。

**语义提醒**：中立怪不是"点一下就变敌人"，而是**首次受到伤害**时
`Unit.damage()` → `setTarget(from)` → `EnemyUnit.setTarget()` 把 `camp` 翻成 `enemy` 才反击
（`unit.ts:602-614`、`enemy-unit.ts:455-460`）。见 `AGENTS.md` **§13**。

**验证**：
- 单测：`web` +3（`isAttackableCamp` 边界；推送含中立单位的 tick 后
  `enemies`/`neutrals`/`attackables` 三个 getter 的归属）。
- 端到端：真实服务器上注册新号 → 完成剧情 1 → 进 `town.street` → 等大史莱姆刷出（warmup 120s）
  → `battle.focus` 点它 → 观察其 `camp` 由 `neutral` 翻成 `enemy` 且我方 `targetId` 指向它。

---

## 2.2 ✅ 已修复：一个账号同时存在多个角色会话 → 串号 + 双份推送

**症状**：同账号开两个标签页时，「A 里看到的/被推进的是 B 选的角色」，且 tick 推送成倍增长
（上一个问题里"实测 10 帧/秒"就是这个原因的一部分）。

**根因（三处叠加）**：
1. `player.select` 只 `world.start(新角色)`，**不停旧角色的会话** → 两个角色的世界同时 tick；
2. 框架的定向推送 `sendNotification(userId, …)` 会发给该 userId 的**全部 OPEN 连接**；
3. 「当前角色」`WorldService.activeByUser` 是**按 userId 全局**的，且
   `world/battle/idle` 三个 Action 各自写 `body.key ?? activeCharacterOf(userId)`（fail-open），
   而前端 transport 的 `world.snapshot/leave/skipOffline/idle.report/claim` **压根不带 key**。

**实测复现**（修复前）：`A 选 X、B 选 Y → A 不带 key 的 world.snapshot 解析到 Y；
2 秒内 tick A=9 B=10 且 9 帧的 serverTime 完全相同`。

**修法**（无框架改动）：
1. `PlayerLogicService.select`：`start(新)` 后 `stop(旧)` → 同一账号**只有一个活跃角色会话**；
2. `WorldService.stop()` 在指针仍指向该角色时清理 `activeByUser`（切人顺序是
   `start(新) → stop(旧)`，不能无条件清）；
3. 新增唯一入口 `WorldService.resolveActiveCharacter(userId, rawKey)`：**显式 key 必须等于当前角色**，
   否则 `PLAYER_NOT_FOUND('该角色不是当前选择的角色')`；不给 key 才回退当前角色；
   未选角一律拒绝。`world.action` / `battle.action` / `idle.action` 全部改走它。

⇒ 推送到该账号任何连接的消息**只属于当前角色**（原版就是单存档单玩家）。

**验证**：
- 单测 `server/test/character-switch.test.ts`（6 例）。把"停旧会话"去掉 → **3/6 失败**（已实测）。
- 端到端 `server/scripts/character-scope-smoke.mjs`（真实 REST+WS+DB，同账号两条连接）
  **7/7 通过**：非当前角色的显式 key 被拒、不带 key 回退当前角色、tick 帧里只有当前角色、
  两个连接各 ~5 帧/秒且内容一致。
- 见 `AGENTS.md` **§15**。

**追加修复（用户实测反馈）**：刷新页面后停在选角页仍收到 `(world, tick)`。
根因是「当前角色」在服务端不随新连接失效 + `OnlineSessionService.isOnline` 是账号级判据。
已改为**每次 WS 握手成功即 `await WorldService.resetActiveCharacter(userId)`**
（停会话 + 清注册表 + `batcher.drop` 丢弃那批属于上一段游玩的待发帧，2s 超时保护），
前端在「重连」（曾经 online 过之后再次 online）时重新 `select`。
冒烟扩到 **10/10**：新连接（未 select）收到 **0** 帧、`world.snapshot` 报「尚未选择角色」。

**仍待做（同一类问题的剩余面）**：面板域（inventory / bank / shop / career / produce /
lootrule / story）的 `characterId` 仍是可选 + `PanelCharacterService` 按 userId 存当前角色，
没有做同样的显式化与归属校验 —— 也就是"同账号两个连接可能把面板操作落到同一个当前角色上"
（比世界/离线那条链轻，但同类）。要做的话：面板 Action 显式带 `characterId` 并校验归属，
或给面板请求也吃 `resolveActiveCharacter`。

---

## 2.3 ✅ 已修复：数据层用原版属性名 `unit.timeline` → 运行期 `undefined.pause()`

**症状**（用户实测告警）：`WorldService` 打印
`world tick 失败：<characterId>` / `{ "reason": "Cannot read properties of undefined (reading 'pause')" }`，
且 `freezed`（冻结）/`stunned`（昏迷）/`transform` 这些 debuff **实际不生效**。

**根因（与 §11 的 `lootRule` 类型撒谎、`sendSkillUsage` 缺失同源）**：数据层函数体的 `this` 在
`contracts/data.ts` 里被冻结成 `unknown`，而 `data/_shapes.ts` 的**视图类型**把
`UnitLike.timeline` 声明成存在。真实内核对齐表在 `combat/unit.ts` 顶部已写明
「原版 `this.timeline` → `Unit.clock`」，于是 `this.unit.timeline.pause()` 编译通过、
运行期 `unit.timeline` 是 `undefined` → 抛错 → 被 tick 的 per-session catch 吞成一条 WARN。

**修法**：
1. `data/buffs.ts` 6 处 `this.unit.timeline` → `this.unit.clock`；
2. `data/packages/year2018.ts` 1 处 `this.timeline.setTimeout` → `this.clock.setTimeout`（legend hook，`this` = Unit）；
3. **收口视图类型** `data/_shapes.ts`：`UnitLike.timeline` → `clock`（与真实 `Unit` 一致）——
   这一步让 `tsc` 成为门禁：数据层再写 `.timeline` 会**编译失败**，而不是运行期静默。

**验证**：新增 `combat/buff-timeline.test.ts`（`freezed` 的 `didAppear` 暂停单位时钟、到期
`willRemove` 恢复；`stunned` 的 `didRemove` 恢复且不抛错）。**修复前该用例必失败**（抛 `undefined.pause`）。
`pnpm run verify` = 0（1216 用例）；数据库冒烟全绿（在 3101 上另起实例，未打扰用户 3100 的服务）。

> 教训：**数据层视图类型（`_shapes.ts`）必须与真实内核成员名一致**，否则它就是下一处
> "类型撒谎"。凡是原版属性名在移植中被改名（`timeline→clock`、`sendSkillUsage` 等），
> 都要在视图类型里用**新名**，让编译器兜底。

---

## 2.4 ✅ 已修复：数据层调用了内核上**不存在的方法/属性**（`sendGeneralMsg`、`SkillState.summoner`）

顺着 §2.3 的教训做了一次**机械化审计**（用 TypeScript 编译器 API 把 `_shapes.ts` 每个视图
接口的成员与真实内核类逐一对照），又揪出两处同类缺陷：

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| a | `enemies.ts` 里 **26 处**机关提示 / BOSS 对话（「机关被触动了」「科力克：…」）玩家永远看不到；在线被 tick 的 try/catch 吞成一条 WARN | `WorldLike.sendGeneralMsg` 被声明成存在，但 `BattleWorld` 上**没有这个方法**——数据层文风来自原版，移植时漏掉了适配器 | 在 `BattleWorld` 上补 `sendGeneralMsg(msg)`，转发到**既有**端口 `BattleSink.general({text})`（与 `enemy.appear` / `player.death` 同一条出站路径）。数据层保持原版写法不动 |
| b | `year2018.heal`（年兽幼崽的治疗）**永远静默不生效** | `effect` hook 里写的是 `this.summoner`，但引擎调用它时 `this` 是 **`SkillState`**（`SkillState.effect` 的 `skillData.effect.call(this, world, unit, level)`），而 `summoner` 只存在于 `Unit` ⇒ 恒 `undefined`，再被 `if (!target) return;` 吞掉 | 改为从第二参数（施法单位）取 `self.summoner`，与同条目 `canUse` 的写法一致 |

**顺带收口的视图类型撒谎**（内核上没有、数据层也从未使用，纯粹是放宽了 `tsc`）：
`UnitLike.race / career / iceAbsorb / hasBuff / stopped`、`BuffStateLike.level / remove`、
`SkillStateLike.summoner / runAttrHooks`、`WorldLike.time`；并把 `UnitLike.player` 从
`UnitLike`（错：那是角色对象）拆成独立视图 `PlayerView` → `PlayerLike`。

**根治措施（防复发，本次新增）**：新增 **编译期门禁** `packages/game-core/src/data/_shapes.gate.ts` ——
用 `type MissingOn<View, keyof Kernel>` + `AssertNoMissing<T extends never>` 断言 7 组视图
（`UnitLike`→`Unit|PlayerUnit|EnemyUnit`、`BuffStateLike`→`BuffState`、`SkillStateLike`→`SkillState`、
`WorldLike`→`BattleWorld`、`PlayerView`→`PlayerLike`、以及嵌套的 `TimelineLike`→`Clock`、
`DataRngStreams`→`CombatRngStreams`）的每个成员都真实存在。**视图再撒谎 = `pnpm run typecheck` 直接失败。**

> ⚠️ 该文件**不能**叫 `*.test.ts`：`packages/game-core/tsconfig.json` 的 `exclude` 里有
> `src/**/*.test.ts`。第一版放进测试文件后，把 `timeline` 塞回视图 typecheck 依然 exit 0 ——
> **假门禁**。已实测：注入 `timeline` / `time` / 任意伪造方法 / `SkillStateLike.summoner`
> 四种形态，门禁都会以 `TS2344` 点名该成员失败。

**验证**：
- `game-core` 新增 `combat/kernel-adapters.test.ts`（6 例：`sendGeneralMsg` 透传/边界/与真实
  `enemy.appear` 同通道、`year2018.heal` 治疗召唤者、`canUse` 25% 门限、无召唤者的安全路径）。
  **回退这两处修复后 4/6 失败**（`sendGeneralMsg is not a function` ×3，
  治疗用例 `expected 1 to be greater than 1`），已实测。
- `server` 在既有的 `test/world-simulation.test.ts` 里补 3 例，用**服务端真实的 `BattleCollector`**
  把「数据层 → 内核适配器 → 服务端出站事件」整段链路钉死（含与 `enemy.appear` 共用一条
  `general` 通道、空串/数字/null 边界）。
- 端到端：`chapter4.sanAnthony2` 的机关提示需要 `chapter3-7` 前置剧情，冒烟走不到，
  因此该条提示**未做真机走到**；但链路两半分别由上面两类测试覆盖，
  且 `general` 类型本就在 `WorldTickDto['events']`（`protocol/src/dto.ts:298`）。

---

## 3. 🟠 协议缺口（3 条，都是「定义了但没接通」或「只在本地类型里」）

| # | 缺口 | 位置 | 修法 |
|---|---|---|---|
| 3.1 | `RebuildCostsDto` 已定义但**没有任何 typed API 返回** → 重铸页不显示价格 | `protocol/src/dto.ts`、`ionet-transport/src/api/game-api.ts` | 在 `produce` 段新增 subCmd（如 `rebuildCosts`）或让 `enchantCosts` 一并返回；服务端已有 `rebuildCostFor(player,id)` |
| 3.2 | `PlayerExportSaveDto` / `PlayerImportSaveInput` **只在 transport 本地声明** | 同上 | 收进 `protocol/src/dto.ts`（服务端已做 `content`/`save` 双向别名兼容） |
| 3.3 | 推送载荷形状未冻结：**`story.unlock` 已冻结**为 `StoryUnlockDto`（见 §2）；仍剩 `inventory.changed` / `career.levelup` | `protocol/src/dto.ts` | 定义正式 DTO；前端目前是防御式处理（数组则整体替换，否则刷新） |

---

## 4. 🟡 前端未发送 `opId`

服务端**已实现并测试**了消耗类操作的 `opId` 幂等（`OpIdempotencyService`，
`DUPLICATE_OPERATION`），但前端调用点没带这个字段 → 双击/网络重试仍可能重复提交
（目前只靠限流兜底）。改 `ionet-transport` 的 `game-api.ts` 对应方法 + `web` 调用点即可（可选字段，不改形状）。

---

## 5. 🟡 数值与实现待产品确认

- `playerSlotMax = 24`、`diamonds→gold = 1:100`、`diamonds→medicine = 50/级` —— 都是我定的显式常量
  （原版无存档化定价），需要拍板。
- `bowelLevel`（坩埚）/ `purchaseRate`（神力购买倍率）在 `PlayerAccountState` 里**没有字段** → 分别恒 0 / 跳过奖励。
  要真做需扩 `PlayerAccountState` 与账号状态持久化。
- `inventory.expand` **只实现了神力路径**，原版的金币/材料路径（`upgrades.inventory`）未实现。
- `combat/enemy-unit.ts:442` 有 `TODO: 包裹已满丢弃物品`（掉落时包裹满的处理）。

---

## 6. 🟡 测试与验收的已知盲区

- **前端没有 DOM 交互测试**：仓库未装 `jsdom` / `@testing-library`，web 的测试是
  store 级集成 + `react-dom/server` 冒烟。**已补「有数据」渲染冒烟**（`ui-smoke.test.tsx`），
  因为空数据冒烟抓不到只在非空分支执行的代码（`BattlePanel` 的 `unlockedCount` 就是这样漏过的）。
  要做真交互测试需新增依赖，需要决定。
- **Eyer 主线的「进图推进」已端到端验证**（`story-entry-smoke.mjs` 14/14：剧情 1 → 进 town.street
  → 剧情 2 自动播放 → 剧情 3 击杀任务登记 remaining=10）。数据表里共 33 条剧情，
  `Aleanor` 那条链与 Eyer 后半段（`eyer-stories-4+`，需要进地城）**没跑过**。
  建议用 `demo` 账号建一个 `Aleanor` 角色走一遍。
- **前端仍未与真实服务端做过完整人工联调**（只做过接口/代理层验证 + store 级集成）。
  调试环境现在**是启动着的**（后端 3100 / 前端 5274，见 §7）。

---

## 7. 恢复本地调试

端口与代理地址的**唯一真相**是仓库根的 `dev.config.json`（前端 `vite.config.ts` 直接 import，
后端 `scripts/dev.mjs` 读它并写进 `process.env.PORT`）。因此命令里**不再需要**端口参数：

```bash
pnpm dev:server    # 后端 3100（tsc --watch + node --watch）
pnpm dev:web       # 前端 5273（vite），/api 与 /ws 代理到 3100
pnpm dev           # 两者一起（--parallel）
```

临时换端口不改文件：`PORT=3200 pnpm dev:server`、`VITE_DEV_PORT=5300 pnpm dev:web`、
`VITE_BACKEND_ORIGIN=http://127.0.0.1:9999 pnpm dev:web`。
详见 `AGENTS.md` **§7.9**。

**开发环境角色经验倍率 = 10**（`dev.config.json` 的 `expRate`，由 `pnpm dev:server` 注入
`EXP_RATE`，启动日志会打印 `角色经验倍率 10`）。它只放大**角色经验**，不影响技能经验与掉落数量；
**只有走 `pnpm dev:server` 才有倍率**（`node dist/main.js` / CI / 生产一律 1）。
实测：单只小史莱姆给 80 经验（基础 8 × 10）。想临时关掉就 `EXP_RATE=1 pnpm dev:server`。

- 账号：`demo` / `demo123456`（无角色）；或直接在登录页「注册」新建（该功能已加）。
- 推送频率自检（dev-only）：浏览器 Console 里 `await __idleDarkTickRate()` →
  打印 `(world, tick)` 的真实帧率/间隔/重复投递数。设计值 **5Hz/连接（200ms）**，
  已实测 5.00/s、间隔中位数 200ms；若看到更多，多半是**多个标签页/多个 dev server**
  导致同一帧投递多次（见 `AGENTS.md` §14）。
- **别用 3000 端口做验证**：那是 **xiuxian 自己的服务端**（响应无 `service` 字段、会报 `redis`）。
  代理后可用 `curl http://127.0.0.1:5273/api/health` 自检 —— 含 `service: "idle-dark-forever"` 才是对的。
- 起服务端前先 `pnpm run build`（`route-probe` / `game-flow-smoke` 跑的是 `dist`）。
- ⚠️ **改了前端文件后若浏览器行为与代码不符**：先 `touch` 该文件，仍不行就重启 Vite（`AGENTS.md` §7.5）。
  验证纪律：`curl` dev server 吐出的模块并 grep **新符号**，不要只看 200。
- ⚠️ **端口被占**：前端是 `strictPort`，占用了就直接失败、不静默换号。
  排查用 bind 探测而**不要**看 `ss`（`AGENTS.md` §7.8：每次 bash 调用在独立 PID namespace，
  `ps` 看不到别的调用的进程，`kill` 也够不到）。
- 本次实测：**5273 上有一个不是本会话启动的 Vite 实例**在服务（大概是更早一次调试的残留，
  我看不到它的 PID，因此无法重启它）。它服务的是最新磁盘代码、REST/WS 代理都通
  （`node scripts/ws-protocol-smoke.mjs 5273 dev-secret-change-me` = **15/15**）。
  若要一个**可被我重启**的前端，把 `dev.config.json` 的 `frontendPort` 改成 5274 再 `pnpm dev:web`。

## 8. 待清理 / 待确认

- 🟡 **空闲会话不回收（内存）**：角色离线后 `WorldService.sessions` 里的会话**不自动释放**，
  只有 `stop()`（切人 / `world.leave` / 新 WS 握手重置）才删。直接关掉浏览器且不再回来的账号，
  其会话（含整个 `BattleWorld` 与单位）会一直挂在内存里；每 200ms 的 tick 仍会遍历到它
  （`isOnline` 判据后即返回，**不做战斗计算**，见下）。
  要真做：加一个空闲回收（离线超过 N 分钟 → `stop()`），或按 LRU 限制会话数。
- ✅ **离线不做战斗计算**（已核实并加单测）：`tickSession` 首行 `if (!isOnline(...)) return;`
  → 不 `stepPaused`（暂停态的 `ClockBase` 已 `clearWake()`，自身不会跑；`stepPaused` 在非暂停态
  直接抛错，只有 tick 循环能推进世界）。离线收益由 `idle` 域在玩家回来时**一次性**用虚拟时钟快进
  （30min 模拟预算 + 速率外推，72h 上限）。
  证据：`world.service.test.ts` 的「离线时世界不推进」用例（离线 20 tick 后
  `units.length`/`exp` 完全不变；在线对照会变）；去掉那行早退 → 2 个用例立刻失败（已实测）。


- `packages/server/tmp/` 下有我排查用的临时脚本（含 `neutral-focus-check.mjs` —— 验证中立怪可点选参战，值得留着复用）（`debug-progression` / `debug-story-flow` /
  `debug-kill` / `debug-repro` / `debug-demo` / `verify-fix` / `route-probe` / `neutral-focus-check`）。
  该目录已 gitignore、**未入库**；`verify-fix`（验证剧情地图门控）与 `route-probe`（路由探针）值得留着复用，其余可删。
- **用户现有角色（账号 `nbb01`）的存档**：`storiesMap` 里 `eyer-stories-2` 是**修复前在 home 完成**的
  历史痕迹。它**不影响后续推进**（`eyer-stories-3` 已是 `task`，去 `town.street` 打 10 只史莱姆即可完成）。
  当时问过是否要把它回退成未完成以便按原版顺序重走，**用户未回答**——若需要，改 `account_state.data.storiesMap` 即可。
- **Redis 未接入**（`.env` 里有 `REDIS_URL` 但 `app.module` 是 `redis: false`，在线状态走内存注册表）。
  单进程部署不需要；要跨实例推送/分布式会话时才需要打开。
