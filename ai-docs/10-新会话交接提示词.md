# 新会话交接提示词

> 用途：开新会话继续本工程时，**整段复制**对应提示词即可。分两个会话：
> **A = 本仓实施会话**（idle-dark-forever，拍板后开工）；**B = 框架补齐会话**（ionet-ts 逻辑服运行时）。
>
> 当前状态：**只产出方案，未改代码**；HEAD `efb1655`，工作树仅 `ai-docs/07`、`08`、`09` 三份未跟踪文档。

---

## A. 本仓实施会话（推荐先开这个）

```text
接续工程 /home/nbb/projects/idle-dark-forever（《永夜2016典藏重置版》服务端权威重制版）。

【基线】
- HEAD == origin/main == efb1655，工作树干净（仅 ai-docs/07、08、09 三份未跟踪新文档，未改任何代码）。
- pnpm run verify = 0（1079 用例：protocol 21 / game-core 400 / transport 73 / ui-kit 194 / server 315 / web 76）。
- 数据库冒烟基线：线协议 15/15、路由 20/20、完整流程 22/22、拾取规则 18/18、进图剧情 14/14、角色归属 10/10。
- 独立库 idle_dark（凭据在 packages/server/.env，gitignore）；绝不把本工程表建进 xiuxian 的 idle_game。
- 端口从 dev.config.json 读（后端 3100 / 前端 5273）；3000 是 xiuxian 的服务端，别打别杀。
- vendor/ionet-ts 是指向 /home/nbb/projects/ionet-ts 的符号链接；本仓禁止改它。

【先按顺序读，别跳过】
1. AGENTS.md（硬约束 §1、线协议 §5、环境坑 §7、§11–§15）
2. ai-docs/05-交接-未竟事宜.md（§0 基线 + §2.2 残项 + §3–§8 待办）
3. ai-docs/06-容量基准与并发上限.md（实测 CPU/帧/内存/DB 数字）
4. ai-docs/07-容量限定与迁移任务书.md（7 个待拍板参数 + 可观测指标 + 超载阶梯；量化附录）
5. ai-docs/08-逻辑服迁移任务书.md（逻辑服边界总纲 + 分阶段任务 + 门禁）
6. ai-docs/09-地图与秘境逻辑服.md（地图/秘境控制器；**已拍板决定台账在 §0.1、清单在 §10.1**）

【已拍板，不要再问】
- A1/A2：先做**单进程逻辑服边界**；框架运行时交另一会话
  （证据已写入 ionet-ts/ai-docs/logic-server-runtime-requirements.md）。本仓不做跨进程。
- RA1 **拓扑 B**：battle = 唯一战斗仿真器 + 会话宿主；map = 开放世界控制器；dungeon = 秘境 + 挑战队列控制器。
- RB2 **不采用 room 模型**（理由见 09 §1，别再提）。
- RC1 无尽（nightmare）暂缓，登记为已知死机制；RC2 秘境票**进图扣一次**（要修"双扣票"）；
  RC3 **实装** resetPrice/coolDown（每日 4 点重置、神力买钥匙）；RC4 挑战队列由**服务端唯一所有**。
- RD1 离线**只在战斗区域**结算；RD2 离线击杀**计入**剧情击杀任务；
  RD3 秘境**支持离线** + 支持挑战队列 + **全部秘境完成后自动挑战非秘境地图**。
- idle（离线结算编排）归属 **dungeon 控制器**（修正 08 B2 的原建议）。

【仍需拍板，动代码前先问用户】
- 09 §10.2：RD4 队列耗尽后进哪张非秘境图（建议 outside → 持久化位置 → home）；
  RD5 队列中秘境无票/冷却未就绪时**跳过并继续**（建议）还是中断；RD6 每日 4 点重置的**时区**；
  RD7 离线预算耗尽**停在当前条目**、禁对秘境做速率外推（建议）是否接受。
- 08 §6 其余：B1 六服划分、B3 career 归属、B4 external 为唯一权威、C1 characters.state 分区+乐观锁、
  C2 背包唯一写者=item、C3/C4、D1–D2（注意 dev.config.json 的 expRate 现为 100，而文档写 10）。

【开工顺序（拍板后）】
按 08 §4 + 09 §7 的依赖顺序：
R0 登记 → R1 battle 会话宿主与预算驱动 tick（含 07 T-A2：去掉 maxCharactersPerTick 人数语义 + 全局回调预算）
→ R2 map → R3 dungeon（票/冷却/队列/run 落库，修 M2/M5/M7）→ R4 battle 命令/事件对接
→ R5 离线结算编排（战斗区域判定 + 队列顺序模拟 + 击杀计入任务 + 幂等）。
每条都按文档里的"改动面 / 验收（边界单测 + 冒烟脚本 + 数字门槛）/ 回滚开关"执行。

另需新增架构门禁 packages/server/test/logic-server-boundary.test.ts：
无环（含 world/story/inventory 三条已知环的回归）、禁止跨服深路径 import、cmd 段唯一归属、
*/logic-server.ts 内不得有 @ActionMethod。

【硬约束（违反即不合格）】
- 提交信息禁止 Co-Authored-By 签名。
- 改代码后必须 pnpm run build + pnpm run typecheck 通过；补边界单测
  （undefined/NaN/0/负数/空数组/超大值/时钟回拨/错误路径）；全量门禁 pnpm run verify。
- 工作量预估禁止任何时间单位（用规模/门禁/依赖顺序/风险）。
- 沙箱坑：/tmp 每次 bash 调用重置（日志写工作区 tmp/）；pnpm dev:server 在跑时别手动 build（抢写 dist）；
  改前端后必须 curl dev server 并 grep 新版本才有的运行期符号（不能只看 200）。
- 推送设计值 5 帧/秒；浏览器 Console 里 await __idleDarkTickRate() 自检。

【工作方式】
- 先给结论/计划再动代码；每个任务独立提交，交付时附 pnpm run verify 结果与冒烟数字。
- 文档与代码冲突时以代码实测为准，并回写文档。
```

---

## B. 框架补齐会话（ionet-ts 逻辑服运行时）

```text
接续框架仓库 /home/nbb/projects/ionet-ts（ionet 的 TypeScript 移植；ionet/ 是 Java 原版，packages/ 是 TS 移植）。

【任务】
把 TS 移植补齐「逻辑服运行时」，供消费工程 idle-dark-forever 拆分
battle/map/dungeon/item/quest/character + 对外服。

【现状（已审计）】
TS 目前是「N 个完全相同的对外服实例 + Redis 会话/广播/房/锁」，**不是**「1 对外服 + N 逻辑服」：
没有 LogicServer 宿主与服务器元数据/注册表、没有外部服→逻辑服路由、没有跨服 call/send、
没有 OnExternal、没有 Redis request/reply；extension-nestjs 的 redis 只 connect()、
广播恒为 MemoryBroadcaster（DistributedBroadcasterDecorator 从未接线）。

【先读】
- ai-docs/logic-server-runtime-requirements.md ← **需求证据包**（E1–E11 现状证据含行号、Java 对照表、
  RS1–RS9 按依赖排序的缺口/落点/验收，以及"明确不需要 room 等能力"的结论）
- CLAUDE.md（提交禁 Co-Authored-By；改代码后必须 pnpm -w run build）
- Java 原版可对照：ionet/net-server、ionet/net-logic-server、ionet/net-center、ionet/run-one、
  ionet/core-framework/.../communication/**
- ionet 官方知识库 ionet-ai（rules/ + stable/ 为权威层）：
    cd /home/nbb/projects/ionet-ts && mkdir -p tmp/uv-home tmp/uv-cache
    HOME=$PWD/tmp/uv-home UV_CACHE_DIR=$PWD/tmp/uv-cache \
      uv run --project /home/nbb/projects/ionet-ai ionet-ai kb search "LogicServer"
  硬规则见 rules/logic-server-rules.md：*LogicServer 命名、放模块根包、两个 builder 方法、
  不得写业务、一个 scanActionPackage 一个 Action 类。

【按 RS1→RS9 实施】
RS1 LogicServer 宿主（无客户端端口的逻辑服进程）
→ RS2 服务器元数据 + 注册/发现（serverId/name/tag/cmdMerges + Redis）
→ RS3 分布式路由表（外部服按 cmdMerge 转发；未注册路由显式报错）
→ RS4 Redis 请求/响应 RPC（correlation id / pending future / 超时；当前只有 fire-and-forget）
→ RS5 FlowContext.call/callAsync/send
→ RS6 OnExternal + 跨进程连接可见性（接线 DistributedBroadcasterDecorator）
→ RS7 extension-nestjs 分布式接线（redis 目前只 connect；广播恒内存；redis 不接受 true）
→ RS8 跨进程重复路由检测；（RS9 callCollect / 分布式 EventBus 可选，消费方当前不需要）
**明确不需要**：room 房间模型与范围广播、Center Server、Aeron。

【验收要求】
每条都要多进程（2 实例 + 1 对外服）集成测试 + 失败路径测试
（超时 / 对端崩溃 / 未注册路由 / 重复回包 / 实例下线）；未注册路由必须显式报错，不得静默 404 掩盖。

【约束】
- 提交信息禁 Co-Authored-By；改代码后 pnpm -w run build 必须通过。
- 不要改 idle-dark-forever 仓库（只读其文档）。
```

---

## C. 两个会话的关系

- **A 不依赖 B**：A 全程按单进程边界推进（A2 已拍板），B 补齐后再开一个"多进程部署"会话（08 阶段 7 / 09 R8）。
- B 的产出（逻辑服运行时）**不改 A 的接口设计**：A 的 `<server>/logic-server.ts` seam 与
  "跨服只走 call/send/事件"的约定就是为 B 预留的，B 完成后替换实现即可。
- 若要走跨会话协作流程（可勾选清单 + 独立复核）：可加载 `cross-session-collaboration` /
  `cross-session-verification` 两个 skill，按"一协作一文件"建 `protocol_<时间戳>_<序号>.md`。
