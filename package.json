{
  "name": "idle-dark-forever",
  "version": "0.1.0",
  "private": true,
  "description": "永夜 · 服务端权威重制 — ionet-ts + NestJS + React + Antd",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter './packages/*' run dev",
    "dev:server": "pnpm --filter ./packages/server dev",
    "dev:web": "pnpm --filter ./packages/web dev",
    "build": "pnpm run build:protocol && pnpm run build:core && pnpm run build:transport && pnpm run build:ui-kit && pnpm run build:server && pnpm run build:web",
    "build:all": "pnpm -r run build",
    "build:protocol": "pnpm --filter @idle-dark/protocol run build",
    "build:core": "pnpm --filter @idle-dark/game-core run build",
    "build:transport": "pnpm --filter @idle-dark/ionet-transport run build",
    "build:ui-kit": "pnpm --filter @idle-dark/ui-kit run build",
    "build:server": "pnpm --filter idle-dark-server run build",
    "build:web": "pnpm --filter idle-dark-web run build",
    "typecheck": "pnpm run build:protocol && pnpm run build:core && pnpm --filter './packages/*' run typecheck",
    "verify": "pnpm run build && pnpm run typecheck && pnpm run test",
    "test": "pnpm --filter './packages/*' run test",
    "test:all": "pnpm -r run test",
    "clean": "pnpm --filter './packages/*' run clean"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
